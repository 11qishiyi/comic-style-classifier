# 漫画画风分类数据集 —— 依赖清单
# 分三档装：只跑推理 / 复现数据集 / 完整训练

# ---------- 推理（release/predict.py 的 ONNX 路径，最小集合）----------
numpy>=1.24
pillow>=10.0
onnxruntime>=1.17

# ---------- 复现数据集（下载、清洗、切分）----------
kaggle>=1.6          # 下载 Kaggle 数据集，需 ~/.kaggle/access_token
opencv-python>=4.8   # 图像清洗、分格切分、感知哈希去重

# ---------- 训练与评估 ----------
ultralytics>=8.4
torch>=2.0           # CPU 版即可，本项目的实验都在 CPU 上跑
onnx>=1.16           # 仅在需要重新导出 ONNX 时用到
